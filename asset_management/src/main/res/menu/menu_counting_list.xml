<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:mContext=".view.MainActivityBase">
    <item android:id="@+id/search"
        android:title="@string/search_title"
        android:icon="@drawable/ic_search"
        app:showAsAction="always"
        app:actionViewClass="androidx.appcompat.widget.SearchView"/>
    <!--<item android:id="@+id/sort"
        android:title="@string/sort_title"
        android:icon="@drawable/ic_sort_white"
        app:showAsAction="ifRoom">
        <menu>
            <item android:id="@+id/sort_by_acq_date"
                android:title="@string/sort_by_acq_date_title"
                app:showAsAction="ifRoom"/>
            <item android:id="@+id/sort_by_name"
                android:title="@string/sort_by_name_title"
                app:showAsAction="ifRoom"/>
            <item android:id="@+id/sort_by_assigned_employee"
                android:title="@string/sort_by_assigned_employee_title"
                app:showAsAction="ifRoom"/>
            <item android:id="@+id/sort_by_assigned_room"
                android:title="@string/sort_by_assigned_room_title"
                app:showAsAction="ifRoom"/>
            <item android:id="@+id/sort_by_price"
                android:title="@string/sort_by_price_title"
                app:showAsAction="ifRoom"/>
        </menu>
    </item>-->
</menu>